// import type { GraphQLContext } from "../context";

export const webhookResolvers = {
  Query: {
    // Webhook queries can be added here if needed
    // Currently webhooks are handled through the WebhookService
  },

  Mutation: {
    // Webhook mutations can be added here if needed
    // Currently webhooks are handled through the WebhookService
  },
};
