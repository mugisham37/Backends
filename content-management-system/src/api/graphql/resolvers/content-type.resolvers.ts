// import type { GraphQLContext } from "../context";

export const contentTypeResolvers = {
  Query: {
    // Content type queries can be added here if needed
    // Currently content types are not part of the core simplified architecture
  },

  Mutation: {
    // Content type mutations can be added here if needed
    // Currently content types are not part of the core simplified architecture
  },
};
