// import type { GraphQLContext } from "../context";

export const workflowResolvers = {
  Query: {
    // Workflow queries can be added here if needed
    // Currently workflows are not part of the core simplified architecture
  },

  Mutation: {
    // Workflow mutations can be added here if needed
    // Currently workflows are not part of the core simplified architecture
  },
};
